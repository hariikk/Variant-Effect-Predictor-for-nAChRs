# Required by NCBI Entrez policy — use a real email address.
NCBI_EMAIL=your_email@example.com

# Optional: an NCBI API key raises the rate limit from 3 to 10 requests/second.
# Get one at https://www.ncbi.nlm.nih.gov/account/ -> Settings -> API Key Management
NCBI_API_KEY=
